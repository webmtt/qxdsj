const getters = {
  name: state => state.user.name,
  pass: state => state.user.pass,
  chName: state => state.user.chName,
  pId: state => state.user.pId,
  pName: state => state.user.pName,
  iD: state => state.user.iD
}
export default getters
